#!/usr/bin/python3
def safe_print_division(a, b):
    z = 0
    r = 0
    try:
        z = (a / b)
        print('Inside result: {}'.format(z))
    except ZeroDivisionError:
        return(None)
    finally:
        return(z)

#!/usr/bin/python3
def safe_print_list_integers(my_list=[], x=0):
    s = []
    i = 0
    j = 0
    try:
        for i in range(0, x):
            s.append(my_list[i])
            print('{:d}'.format(my_list[i]),end="")
        return(x)
    except TypeError:
        return(x)

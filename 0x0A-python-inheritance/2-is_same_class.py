#!/usr/bin/python3
"""Module Thats contains a function (is same class)"""


def is_same_class(obj, a_class):
    """is same class function"""
    if (type(obj)) == (a_class):
        return(True)
    else:
        return(False)

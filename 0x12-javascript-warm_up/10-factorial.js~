#!/usr/bin/node
const process = require('process');
function factorial(x) {
    if (parseInt(x) === 0) {
	return (1);
    } else {
	return (parseInt(x) * factorial(x - 1));
}
}
console.log(factorial(process.argv[2], 10) || 0);
